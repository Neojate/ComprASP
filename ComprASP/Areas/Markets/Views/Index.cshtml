<h1>Mercados</h1>
<partial name="_Modal" />
<div class="d-flex justify-content-end mb-4">
    <a class="btn btn-outline-warning" href="create">Crear nuevo</a>    
</div>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Market market in Model)
        {
            <tr>
                <td>@market.Name</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@market.Id" class="btn btn-sm btn-outline-success">
                        <i class="fa-solid fa-pencil"></i>
                    </a>
                    <button class="btn btn-sm btn-outline-dark" onclick="myDelete('/market/delete/@market.Id')" type="button">
                        <i class="fa-solid fa-trash"></i>
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>
@section Scripts {
    <script>
        function myDelete(url) {
            $('#confirm_delete').prop('href', url);
            $('#confirm').modal('show');
        }

    </script>
}
